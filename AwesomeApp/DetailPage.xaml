<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AwesomeApp.DetailPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>

                <Image Source="{Binding Image}" WidthRequest="400" HeightRequest="400" Margin="10,8,10,10" >

                </Image>

                <Label x:Name="insectMatch" Text="{Binding insectMatch}" FontSize="Large" VerticalOptions="Center" HorizontalOptions="Center" TextColor="#120AFE">

                </Label>

                <Label x:Name="isPest" Text="{Binding isPest}" FontSize="Large" VerticalOptions="Center" HorizontalOptions="Center" TextColor="#120AFE">

                </Label>
                <Button x:Name="moreInfoBtn" Text="More Info" Clicked="moreInfoBtn_Clicked" BackgroundColor="#11D42D" TextColor="#120AFE">

                </Button>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>




